package usecases

import (
	"banco_de_espana/entities"
	"errors"
)

func UpdateClient(cl *entities.Client, name string, email string, phone uint64) (*entities.Client, error) {
	return nil, errors.New("Not implemented")
}
